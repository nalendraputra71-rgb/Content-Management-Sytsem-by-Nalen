# Setup & Deployment

1. Make sure to set `GEMINI_API_KEY` and `FIREBASE_PROJECT_ID` in your Vercel Environment Variables.
2. The server will run via `vercel.json` rewrites.
3. Push to GitHub to trigger Vercel deployment.
